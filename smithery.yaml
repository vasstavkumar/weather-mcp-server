
startCommand:
  type: stdio
  configSchema:
    {}
  commandFunction:
    |-
    (config) => ({
      "command": "uv",
      "args": [
        "--directory",
        "/app",
        "run",
        "main.py"
      ]
    })
